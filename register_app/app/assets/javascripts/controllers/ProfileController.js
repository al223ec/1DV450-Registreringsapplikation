"use strict";
var controllers = angular.module('controllers');
controllers.controller("ProfileController", ['$scope', 'flash', '$state', 'userService',
    function($scope, flash, $state, userService) {
}]);
