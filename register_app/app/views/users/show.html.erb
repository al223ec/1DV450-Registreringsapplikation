<% provide(:title, @user.name) %>
<section>
    <div class="container">
        <div class="col-md-8 col-md-offset-1">
            <h2>
            <%= gravatar_for @user %>
            <%= @user.name %>
            </h2>
        </div>
    </div>
</section>