<% provide(:title, 'Alla användare') %>
<section>
    <div class="container">
        <div class="col-md-8 col-md-offset-1">
            <h2>Alla användare</h2>
            <%= will_paginate %>
            <ul class="users">	
				<%= render @users %>
            </ul>
            <%= will_paginate %>
        </div>
    </div>
</section>